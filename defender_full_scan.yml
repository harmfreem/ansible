---
- name: Run full Microsoft Defender scan
  hosts: windows
  gather_facts: no
  tasks:
    - name: Run full Defender scan via PowerShell
      win_shell: Start-MpScan -ScanType Full
      register: scan_result
      ignore_errors: yes  

    - name: Show scan output
      debug:
        var: scan_result.stdout
